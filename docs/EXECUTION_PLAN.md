# 📋 الخطة التنفيذية الشاملة والمفصلة لبناء تطبيق "إدارة مكتب محاماة سوري" (V6.2 Production Blueprint)

> **وثيقة التوصيف المعماري والخطة التنفيذية المفصلة لبناء نظام إدارة وأرشفة مكتب محاماة سوري متكامل.**  
> **التقنية المعتمدة:** `Flutter` + `Drift (SQLite)` + `Riverpod` + `GoRouter`.  
> **المنصة الحالية:** `Windows (Offline-First)` مع التجهيز الهيكلي للتوسع إلى `Android` والربط الشبكي لاحقاً.

---

## 🏛️ أولاً: المبادئ المعمارية والضوابط الأساسية

### 1. دورة الحياة الموحدة للعمليات (Universal Lifecycle Principle)
كل عنصر تشغيلي في النظام (دعوى، مرحلة قضائية، جلسة مرافعة، إجراء إداري، مرحلة تأسيس شركة، مذكرة، أو مهمة كشف/معاينة) يخضع لدورة حياة موحدة لا يمكن تجاوزها:
- **مجدولة (`scheduled`):** موعد مستقبلي تم تحديده ولم يحن وقت تنفيذه بعد.
- **قيد التنفيذ (`in_progress`):** إجراء جاري العمل عليه حالياً من قبل المحامي أو المعقب.
- **تمت / أُنجزت (`completed`):** إتمام العمل يتطلب إدخال ملخص النتيجة والمرفقات إن وجدت. **قاعدة الأتمتة الذهبية:** إذا نتج عن الإتمام موعد جديد (مثل تحديد الجلسة القادمة)، يقوم محرك النظام (`TaskSyncService`) بترحيله وإنشاء مهمة يومية جديدة تلقائياً في جدول `daily_tasks`.
- **مؤجلة (`postponed`):** عند التأجيل، يُلزم النظام بإدخال **سبب التأجيل** و**التاريخ الجديد** (الذي يجب أن يكون لاحقاً للتاريخ الحالي). يبقى السجل محتفظاً بتاريخه السابق في `task_history` وتظهر المهمة مستمرة في الواجهة مع عداد لمرات التأجيل.
- **ملغاة (`cancelled`):** يتطلب إدخال سبب الإلغاء، وتبقى محفوظة في الأرشيف والخط الزمني دون أن تختفي.

### 2. قاعدة النواقص القضائية (Deficiency & Completeness Enforcement)
- **إلزامية تحديد المواعيد:** عند إنشاء أي دعوى، عقد، أو إجراء إداري بدون تحديد موعد الجلسة أو الإجراء القادم، أو في حال غياب حقول جوهرية مثل رقم الأساس المحكمي أو سند التوكيل، يتدخل محرك النواقص (`DeficiencyService`) تلقائياً لإنشاء سجل في جدول `deficiencies`.
- **مراقبة التبويب:** يظهر شريط تنبيه أحمر دائم أعلى ملف الدعوى يشير إلى وجود نواقص، وينتقل الملف تلقائياً ليظهر ضمن فلتر "الملفات الناقصة".
- **المعالجة التلقائية:** بمجرد قيام المحامي باستكمال الحقل المفقود أو إرفاق المستند، يتم إغلاق النقص تلقائياً وتحويل حالته إلى `resolved`.

### 3. مركزية البيانات والتناغم الموحد (Decoupled & Unified Modules)
- **الأشخاص والتوكيلات:** اعتماد جدول `persons` كمركز أساسي لكل الأطراف (موكلين طبيعيين، شركات اعتبارية، خصوم، محامين أخصام، كتاب عدل ومندوبين، وفريق المكتب). يُربط جدول الأشخاص بـ `powers_of_attorney` وبكافة ملفات الدعاوى والعقود.
- **المستندات المستقلة (`Documents` & `DocumentLinks`):** فصل كيان المستند عن علاقات الـ Polymorphic لتجنب ضعف القيود؛ يتم حفظ المستند في `documents` وربطه بأي عدد من الدعاوى أو الجلسات أو الأشخاص عبر جدول `document_links`.
- **المالية الموحدة (`Unified Finances`):** بدلاً من تشتيت المالية في الجداول الفرعية، يتم تجميعها في 3 جداول أساسية: `fee_agreements` (اتفاقيات الأتعاب للموكلين)، `fee_payments` (سندات القبض والدفعات)، و `expenses` (مصاريف الملفات القضائية والإدارية).
- **الخط الزمني الشامل (`TimelineEvents`):** سجل كرونولوجي جامع لكل حركة تقع على الملف (إنشاء، إضافة مرحلة، صدور حكم، دفعة مالية، تأجيل، أو اعتزال توكيل).

---

## 🗂️ ثانياً: شجرة مجلدات المشروع (Clean Architecture)

```text
lib/
├── core/
│   ├── constants/       # ثوابت التطبيق (الألوان، مسار حفظ الملفات AppData/LawOffice/files، التوطين)
│   ├── enums/           # الحالات الموحدة (LifecycleStatus, PersonType, EntityType, PoaType...)
│   ├── utils/           # أدوات تنسيق التواريخ، التشفير، ودوال الطباعة وتصدير PDF
│   └── errors/          # إدارة الأخطاء واستثناءات قواعد البيانات
├── data/
│   ├── database/        # قاعدة بيانات Drift
│   │   ├── schema.dart  # تعريف الجداول الـ 22 والفهارس والقيود
│   │   ├── database.dart# إعداد اتصال SQLite المحلي، والبذور الافتراضية
│   │   └── daos/        # كائنات الوصول للبيانات (CaseDao, PersonDao, TaskDao, FinanceDao...)
│   ├── repositories/    # تطبيق واجهات المستودعات وربطها بالمحركات والملفات
│   └── services/        # محركات المنطق الخلفي (SequenceService, TaskSyncService, BackupService...)
├── domain/
│   ├── models/          # كائنات النطاق الخالصة (Pure Domain Models & Entities)
│   └── repositories/    # واجهات الـ Repositories التجريدية
└── presentation/
    ├── app_theme.dart   # ثيم التطبيق (دعم RTL الكامل، خطوط Cairo/Amiri، ألوان المكتب)
    ├── navigation/      # الملاحة والتوجيه (GoRouter)
    ├── providers/       # إدارة الحالة ومزودات البيانات (Riverpod State Controllers)
    └── screens/         # واجهات المستخدم (الشاشة الرئيسية، الدعاوى، الشركات، العقود، الأرشيف، الإعدادات)
```

---

## 📅 ثالثاً: خطة التنفيذ المرحلية المفصلة (Step-by-Step Roadmap)

### 🚀 المرحلة 0: التأسيس، تهيئة البنية التحتية، وتكوين بيئة المشروع (Project Setup & Architecture Core)
- [x] **0.1 إعداد `pubspec.yaml` وإضافة الحزم المعتمدة:**
  - حزم التخزين وقاعدة البيانات: `drift: ^2.14.0`, `sqlite3_flutter_libs: ^0.5.0`, `path_provider`, `path`.
  - حزم إدارة الحالة والملاحة: `flutter_riverpod`, `riverpod_annotation`, `go_router`.
  - حزم الملفات، الطباعة، والضغط: `archive`, `file_picker`, `open_filex`, `pdf`, `printing`.
  - حزم التوطين والتنسيق: `intl`, `flutter_localizations`, `uuid`, `crypto: ^3.0.5`.
  - أدوات التوليد البرمجي (`dev_dependencies`): `build_runner`, `drift_dev`, `riverpod_generator`.
- [x] **0.2 بناء النواة البرمجية (`lib/core/`):**
  - إنشاء `app_enums.dart` مع محولات Drift (`EnumIndexConverter`) لكل من: `LifecycleStatus`, `PersonType`, `PersonRoleType`, `TeamPosition`, `PoaType`, `EntityType`, `TaskPriority`, `DeficiencySeverity`, `DocumentStatus`, `PhysicalLocation`.
  - إنشاء `app_constants.dart` لتعريف ثوابت المسارات وألوان مكتب المحاماة الرسمي، وضبط الاسم الافتراضي للتطبيق "مكتب المحامي" وتحته "هادي فيصل البني" (مع دعم التعديل الديناميكي).
- [x] **0.3 إعداد التوطين والثيم الرسمي (`AppTheme`):**
  - ضبط دعم RTL الكامل للغة العربية كافتراضية مطلقة في التطبيق وتضمين خطوط Cairo و Amiri.
  - بناء نظام الملاحة `app_router.dart`، ومزود الحالة `officeSettingsProvider`، والشاشة الرئيسية `MainLayoutScreen`.

---

### 🗄️ المرحلة 1: بناء قاعدة البيانات، التشفير، وجداول Drift الـ 22 (`Database Schema & DAOs`)
- [x] **1.1 تعريف جداول النظام والإعدادات (`System & Core`):**
  - `AppSettings`: حفظ ثوابت المكتب والترويسة.
  - `Security`: حفظ التشفير، سؤال الأمان، مهلة قفل الشاشة التلقائي بعد عدم النشاط.
  - `ActivityLog`: سجل حركات المستخدمين.
  - `Backups`: سجل النسخ الاحتياطية ومساراتها وحالاتها.
  - `YearlySequences`: إدارة عداد الترقيم السنوي الفريد لكل عام.
- [x] **1.2 تعريف جداول الأشخاص والأدوار (`Persons & Roles`):**
  - `Persons`: الجدول المركزي الشامل (الأفراد، الشركات، العناوين، الهواتف، والجهات).
  - `LegalEntities`: تفاصيل الممثل القانوني للشركات وسند التمثيل.
  - `PersonRoles` & `TeamMembers` (محامي أستاذ، متمرن، معقب، موظفة مكتب).
  - `OpponentLawyers` & `Notaries` (كتاب العدل ومندوبي فروع النقابة الـ 14).
- [x] **1.3 تعريف جداول الوكالات القضائية (`Powers of Attorney`):**
  - `PowersOfAttorney`: وكالات عامة، خاصة (مع حقل الوصف)، وشرعية.
  - `PoaParties`: أطراف الوكالة (موكل ووكيل).
  - `CasePoaLinks`: ربط الوكالات بملفات الدعاوى.
- [x] **1.4 تعريف الجداول المرجعية (`Lookups`):**
  - `Courts` (المحاكم السورية ودوائرها)، `CaseSubjects`، `PartyRolesLookup` (مدعي، مدعى عليه، طاعن، مستأنف...)، `ContractTypesLookup`، `CompanyTypesLookup`.
- [x] **1.5 تعريف جداول الدعاوى، المراحل، الجلسات، والإجراءات (`Litigation System`):**
  - `Cases`: الملف الرئيسي والدورة التشغيلية.
  - `CaseParties`: ربط الموكلين والخصوم بالدعوى مع تحديد الموكل والخصم الرئيسي.
  - `CasePhases`: التسلسل القضائي (صلح، بداية، استئناف، نقض) مع رقم الأساس وسنة المحكمة.
  - `CaseSessions`: الجلسات المرافعة وتاريخ الموعد القادم وقرارات المحكمة.
  - `CaseActions`: إجراءات الملف الخارجية (تبليغ، كشف، مراجعات).
- [x] **1.6 تعريف جداول تأسيس وإدارة الشركات (`Companies Management`):**
  - `Companies` (شركات أشخاص وأموال)، `CompanyPhases` (دورة حياة التأسيس)، `CompanyManagement` (محاضر اجتماعات، تعديلات، تجديدات)، `CompanyPartners` (الشركاء وحصصهم)، `CompanyDirectors`.
- [x] **1.7 تعريف جداول العقود والقوالب (`Contracts System`):**
  - `Contracts`، `ContractParties`، `ContractReminders` (التنبيهات اليدوية والتجديد التلقائي)، `ContractTemplates` (نماذج Word)، `ContractVersions`.
- [x] **1.8 تعريف جداول الإجراءات الإدارية (`Admin Procedures`):**
  - `AdminProcedures`، `AdminSteps`، `AdminProcedureTypes` (Checklist الجاهزة).
- [x] **1.9 تعريف جداول الأعمال اليومية والمهام (`Tasks & Agenda`):**
  - `DailyTasks` (مهام موحدة تتغذى من الجلسات والتذكيرات والمراحل)، `TaskHistory`.
- [x] **1.10 تعريف جداول المستندات، المالية، النواقص، والخط الزمني (`Storage, Finances, Deficiencies & Timeline`):**
  - `Documents`، `DocumentLinks`، `FeeAgreements`، `FeePayments`، `Expenses`، `Deficiencies`، `TimelineEvents`.
- [x] **1.11 إعداد التشفير والفهارس (`database.dart`):**
  - تفعيل اتصال SQLite المحلي وحقن فهارس البحث السريع (`Indexes`) وبذور البيانات السورية التأسيسية للمحاكم ونقابات المحامين.
  - تشغيل `build_runner` والتحقق من سلامة الأكواد المولدة.

---

### ⚙️ المرحلة 2: بناء محركات المنطق والخدمات الخلفية (`Core Business Logic Engines`)
- [x] **2.1 محرك الترقيم السنوي الآمن (`SequenceService`):**
  - بناء المعاملة الآمنة `db.transaction()` لتوليد أرقام الملفات (مثال: `2026/001`) مع القفل التلقائي لمنع أي تعارض عند التسجيل المتزامن.
- [x] **2.2 محرك أتمتة المهام والخط الزمني (`TaskSyncService` & `TimelineEngine`):**
  - أتمتة توليد سجل في `daily_tasks` عند حفظ جلسة قضائية جديدة أو تذكير عقد، وتسجيل حركة مقابلة في `timeline_events`.
  - إدارة ترحيل الجلسات عند التأجيل مع حفظ السبب وتوثيق السجل في `task_history`.
- [x] **2.3 محرك إدارة النواقص القضائية (`DeficiencyService`):**
  - فحص ملف الدعوى أو الإجراء عند الحفظ؛ كشف غياب حقل `next_session_date` أو رقم الأساس أو سند التوكيل، وإضافة نقص مفتوح `open` في جدول `deficiencies`.
  - ربط المحرك بـ DAOs لتحديث حالة النقص إلى `resolved` فور استكمال الحقول من قبل المستخدم.
- [x] **2.4 محرك إدارة الملفات والمرفقات (`FileStorageService`):**
  - إدارة مجلد التخزين `AppData/LawOffice/files/` وحفظ المرفقات تحت تصنيفات (`cases/{id}/`, `contracts/{id}/`, `companies/{id}/`, `powers_of_attorney/`).
  - دعم قراءة وحفظ قوالب Word (`.docx`) وفتحها برمجياً للتحرير الخارجي أو الداخلي.
- [x] **2.5 محرك النسخ الاحتياطي الذكي والاستعادة (`BackupService`):**
  - بناء Isolate خلفي يقوم بضغط قاعدة البيانات ومرفقات المجلد في ملف `.zip` واحد.
  - تنفيذ الفحص الذكي عند إغلاق التطبيق (النسخ التلقائي مرة واحدة كل أسبوع فقط) ودعم النسخ اليدوي واختيار قرص خارجي (`USB / External HD`).

---

### 📦 المرحلة 3: بناء طبقة المستودعات وإدارة الحالة (`Repositories & Riverpod Providers`)
- [x] **3.1 واجهات ومستودعات البيانات (`Repositories Layer`):**
  - بناء `PersonRepository` لإدارة العملاء والخصوم والمحامين وكتاب العدل.
  - بناء `PoaRepository` لإدارة الوكالات وربطها بالدعاوى.
  - بناء `CaseRepository` لإدارة الدعاوى، جلساتها، وإجراءاتها، مع هندسة دالة **"نقل للمرحلة التالية"** التي تنقل القرار والثبوتيات تلقائياً إلى المحكمة الأعلى.
  - بناء `CompanyRepository`, `ContractRepository`, `AdminProcedureRepository`, `TaskRepository`, `FinanceRepository`, `DocumentRepository`, `LookupRepository`.
- [x] **3.2 مزودات الحالة (`Riverpod State Controllers`):**
  - إنشاء `casesProvider`, `caseDetailsProvider`, `dailyTasksProvider`, `deficienciesProvider`, `financesProvider`, `searchProvider`, `settingsProvider`.

---

### 🖥️ المرحلة 4: واجهة المستخدم - الملاحة الرئيسية، لوحة التحكم، وإدارة الأشخاص والوكالات (`UI Phase 1`)
- [x] **4.1 الهيكل العام والملاحة (`Main Scaffold & GoRouter`):**
  - تصميم شريط الملاحة العربي مع التبويبات الـ 6 الرئيسية (جديد ➕، الأرشيف 📁، الأعمال اليومية 📅، فريق المكتب 👥، البحث 🔍، الإعدادات ⚙️).
  - تصميم نافذة **"عمل جديد"** المنبثقة لاختيار مسار العمل (بدء عمل جديد / أرشفة عمل سابق) وتحديد التصنيف.
- [x] **4.2 شاشات سجل الأشخاص وفريق المكتب (`Persons & Team Screens`):**
  - شاشة القائمة الشاملة مع فلاتر التبويب (موكلون، خصوم، محامو الخصم، كتاب عدل ومندوبون، فريق المكتب).
  - شاشة تفاصيل الموكل (ملف شامل يعرض بياناته، قضاياه، عقوده، وشركاته المرتبطة).
- [x] **4.3 شاشة إدارة الوكالات (`Powers of Attorney Screen`):**
  - إنشاء وكالة جديدة واختيار فرع النقابة من القائمة السورية (دمشق، ريف دمشق، حلب، حمص، اللاذقية...).
  - إرفاق صورة السند وربط الموكل والوكيل.

---

### ⚖️ المرحلة 5: واجهة المستخدم - نظام الدعاوى وتبويبات الملف التسعة (`UI Phase 2 - Litigation System`)
- [x] **5.1 معالج إنشاء دعوى جديدة (`Create Case Wizard`):**
  - بناء خطوات التسلسل الإلزامي: الموكل ← الوكالة ← التصنيف ← البيانات الأساسية ← الموضوع والطلبات ← الخصم ← المرفقات ← ★ تحديد موعد التنفيذ القادم ★.
- [x] **5.2 شاشة ملف الدعوى الموحد (`Case Details Screen`):**
  - **شريط الحالة العلوي:** تصميم شريط ثابت يعرض رقم الملف السنوي، المحكمة، المرحلة، إشعار النقص (بالأحمر)، وموعد الجلسة القادمة.
  - **التبويب 1 (الملخص):** نظرة عامة وبطاقات سريعة لإضافة جلسة فورية أو إرفاق مستند.
  - **التبويب 2 (الأطراف والتوكيلات):** عرض الموكلين، الخصوم، الوكالات، ومحامي الخصم.
  - **التبويب 3 (المراحل القضائية):** التسلسل القضائي وزر **"نقل للمرحلة التالية"** مع معالج تحديد المحكمة ورقم الأساس الجديد.
  - **التبويب 4 (الجلسات والإجراءات):** جدول الجلسات المرافعة وحضور الأطراف، وتحديد المطلوب للجلسة القادمة، وجدول الإجراءات الخارجية.
  - **التبويب 5 (المستندات):** قائمة مفلترة حسب جهة الإصدار مع تحديد موقع الأصل الفيزيائي.
  - **التبويب 6 (المالية):** بطاقات أتعاب الموكلين (منفصلة لكل موكل) وبطاقة مصاريف القضية.
  - **التبويب 7 (النواقص):** عرض حقول النقص في الملف وإمكانية الاستكمال أو التجاهل مع التسجيل.
  - **التبويب 8 (الخط الزمني):** شجرة الأحداث الكرونولوجية للملف.
  - **التبويب 9 (الإنهاء):** شاشة إصدار الحكم النهائي أو اعتزال التوكيل مع فرض إرفاق صورة الحكم.

---

### 🏢 المرحلة 6: واجهة المستخدم - الشركات، العقود، والإجراءات الإدارية (`UI Phase 3 - Companies, Contracts & Procedures`)
- [x] **6.1 شاشات تأسيس وإدارة الشركات (`Companies Screens`):**
  - إنشاء شركة عبر مسار أشخاص (عقد) أو أموال (نظام أساسي).
  - بناء تبويبات ملف الشركة الـ 8: البيانات الأساسية والعقار، الشركاء والإدارة، مراحل التأسيس (10 خطوات بدورة حياة كاملة)، إدارة ما بعد التأسيس (اجتماعات، تعديلات، انحلال)، المستندات، المالية، النواقص، والخط الزمني.
- [x] **6.2 شاشات العقود ونماذج Word (`Contracts & Templates Screens`):**
  - إنشاء العقد وربطه بالمتممات المالية والتنبيهات.
  - بناء تبويبات ملف العقد الـ 7، مع تضمين محرر داخلي وزر **"فتح في Word"** للتحرير الخارجي عبر نماذج القوالب.
  - شاشة إدارة قوالب Word وتعيين القالب الافتراضي.
- [x] **6.3 شاشات الإجراءات الإدارية (`Admin Procedures Screens`):**
  - معالجة معاملات الأحوال الشخصية والعقارية والتجارية مع قائمة خطوات الـ Checklist التلقائية القابلة للتعديل.

---

### 📊 المرحلة 7: واجهة المستخدم - الأعمال اليومية، البحث، الطباعة، والإعدادات (`UI Phase 4 - Agenda, Search, Print & Settings`)
- [x] **7.1 شاشة إدارة الأعمال اليومية (`Daily Tasks & Agenda Screen`):**
  - عرض مهام اليوم، الغد، والمهام المتأخرة مقسمة حسب الأولوية.
  - نافذة "إتمام المهمة" وتحديد موعد الجلسة القادمة، أو "تأجيل المهمة" مع إدخال السبب وتاريخ الموعد الجديد.
- [x] **7.2 شاشة الأرشيف والبحث المتقدم (`Archive & Search Screen`):**
  - بحث فوري وشامل في كل ملفات النظام.
  - فلاتر مخصصة: ملفات اليوم، ملفات ناقصة، جلسة قريبة، بانتظار استكمال الأساس، ملفات منتهية.
- [x] **7.3 شاشة الطباعة القانونية وتصدير PDF (`Legal Printing & PDF Export`):**
  - توليد تقارير الجلسات، المذكرات، والبيانات المالية بتنسيق PDF رسمي مع الترويسة السورية وشعار المكتب وخطوط Cairo/Amiri.
- [x] **7.4 شاشة الإعدادات والأمان (`Settings & Security Screen`):**
  - إدارة بيانات المكتب، الشعار، التوقيع، وتعيين كلمة المرور وسؤال الأمان وضبط مهلة القفل.
  - التحكم بالنسخ الاحتياطي السبوعي وتحديد مسار القرص الخارجي واستعادة البيانات.
  - إدارة السجلات المرجعية للقوائم السورية الجاهزة.

---

### 🧪 المرحلة 8: التدقيق الشامل، الاختبار التكاملي E2E، والتسليم (`Testing, Verification & Delivery`)
- [x] **8.1 التدقيق البرمجي (`Static Analysis & Build Verification`):**
  - تشغيل `flutter analyze` والتأكد من خلو المشروع تماماً من أي أخطاء أو تحذيرات.
- [x] **8.2 اختبار الشد الوظيفي (`Workflow & E2E Stress Testing`):**
  - اختبار دورة إنشاء دعوى جديدة، تحديد جلسة قادمة، التحقق من ظهورها في مهام اليوم، إتمام الجلسة وترحيل الموعد التالي.
  - التحقق من عمل محرك النواقص عند ترك موعد الجلسة فارغاً، وإغلاق النقص عند تعبئته.
  - اختبار الترقيم السنوي الآمن عند إنشاء عدة ملفات متزامنة في نفس السنة (`2026/001` ← `2026/002`).
- [x] **8.3 اختبار التشفير والنسخ الاحتياطي:**
  - التحقق من سلامة قاعدة البيانات المحلية واختبار إنشاء ملف الـ `.zip` الاحتياطي واستعادته بنجاح.
- [x] **8.4 التوثيق النهائي والتسليم:**
  - إعداد توثيق الشفرة ومراجعة تطابق الميزات بنسبة 100% مع ميثاق التصميم V6.2.

---

### 🪟 المرحلة 9: دعم سطح المكتب Windows وتشغيل المشروع عند المستخدم (`Windows Desktop Support`)
- [x] **9.1 إضافة مشروع Windows Desktop الرسمي:**
  - إنشاء مجلد `windows/` الكامل وفق بنية Flutter القياسية (`CMakeLists.txt`, `runner/`, `flutter/`) كي يتعرف Flutter على منصة Windows ولا يظهر خطأ `No Windows desktop project configured`.
  - ضبط اسم الملف التنفيذي `lawyer_office.exe` وعنوان النافذة: **"مكتب المحامي - هادي فيصل البني"**.
- [x] **9.2 تجهيز ملفات التسجيل والإضافات:**
  - إضافة ملفات `generated_plugin_registrant` و `generated_plugins.cmake` الابتدائية مع ترك Flutter يعيد توليدها تلقائياً أثناء `flutter pub get` و `flutter run` حسب الإضافات الفعلية.
  - استثناء ملفات Windows المؤقتة (`windows/flutter/ephemeral/` و `.plugin_symlinks/`) من Git لأنها تُولّد محلياً على جهاز التشغيل.
- [x] **9.3 توثيق إصلاح التشغيل:**
  - توثيق سبب المشكلة الجذري: المشروع كان يحوي كود Dart/Flutter فقط دون مجلد منصة Windows، لذلك رفض Flutter التشغيل على `-d windows`.
  - اعتماد أمر تشغيل واحد للمستخدم يقوم بالسحب القسري من `origin/main` ثم توليد ملفات Drift وتشغيل التطبيق.

---

## 📈 مصفوفة التتبع والتقدم (Progress Tracking Matrix)

| المرحلة | الوصف | الحالة الحالية | النسبة المئوية |
|---------|-------|----------------|----------------|
| **المرحلة 0** | التأسيس، إعداد البنية التحتية، وتكوين المشروع | 🟢 مكتملة (100%) | 100% |
| **المرحلة 1** | بناء قاعدة البيانات، التشفير، وجداول Drift الـ 22 | 🟢 مكتملة (100%) | 100% |
| **المرحلة 2** | بناء محركات المنطق والخدمات الخلفية | 🟢 مكتملة (100%) | 100% |
| **المرحلة 3** | بناء طبقة المستودعات وإدارة الحالة (Riverpod) | 🟢 مكتملة (100%) | 100% |
| **المرحلة 4** | الملاحة الرئيسية، لوحة التحكم، وإدارة الأشخاص والوكالات | 🟢 مكتملة (100%) | 100% |
| **المرحلة 5** | نظام الدعاوى وتبويبات الملف التسعة | 🟢 مكتملة (100%) | 100% |
| **المرحلة 6** | إدارة الشركات، العقود، والإجراءات الإدارية | 🟢 مكتملة (100%) | 100% |
| **المرحلة 7** | الأعمال اليومية، البحث، التقارير، والإعدادات | 🟢 مكتملة (100%) | 100% |
| **المرحلة 8** | التدقيق الشامل، الاختبار التكاملي E2E، والتسليم | 🟢 مكتملة (100%) | 100% |
| **المرحلة 9** | دعم سطح المكتب Windows وتشغيل المشروع عند المستخدم | 🟢 مكتملة (100%) | 100% |

---

## 🧭 خطة مراجعة وإعادة تصميم الواجهات

- تم إنشاء ملف مرجعي مستقل لمراجعة الواجهات شاشة شاشة قبل أي تعديل برمجي جديد: [`docs/UI_REVIEW_AND_REDESIGN_PLAN.md`](UI_REVIEW_AND_REDESIGN_PLAN.md).
- يبدأ التدقيق من الشاشة الرئيسية / تبويب **جديد ➕**، ثم ينتقل لباقي الشاشات بالترتيب.
- أي تعديل واجهة لاحق يجب أن يمر بمراحل: مراجعة الخطة الأصلية ← مطابقة التنفيذ ← توثيق الانحرافات ← اعتماد التصميم الجديد ← التنفيذ ← الاختبار ← تحديث التوثيق ← الرفع على Git.

---
> **تذكير إلزامي:** يتم العمل على هذه الخطة مرحلة بمرحلة، ولا يُعلن عن اكتمال أي مرحلة إلا بعد استكمال جميع متطلبات **دستور التطوير (`DEVELOPMENT_GUIDELINES.md`)** وتنفيذ أوامر الرفع على Git.

---
## 📝 سجل الإنجاز والتوثيق (Changelog & Documentation Audit)
- **2026-07-09 [مراجعة شاشة الوكالات]:** إضافة مراجعة شاشة الوكالات القضائية إلى `docs/UI_REVIEW_AND_REDESIGN_PLAN.md`، وتوثيق أن المطابقة الحالية تقارب 55%؛ إدخال الوكالة الأساسي موجود، لكن البحث والفلاتر وشاشة التفاصيل وعرض الأطراف والربط الظاهر بالدعاوى وإدارة دورة حياة الوكالة غير مكتملة.
- **2026-07-09 [مراجعة شاشة الأشخاص والجهات]:** إضافة مراجعة شاشة الأشخاص والجهات وفريق المكتب إلى `docs/UI_REVIEW_AND_REDESIGN_PLAN.md`، وتوثيق أن المطابقة الحالية تقارب 40% بسبب عدم عمل فلاتر الأدوار فعلياً، وعدم فتح تفاصيل الشخص، وبقاء محامي الأخصام وكتاب العدل وفريق المكتب كتبوبيات Placeholder.
- **2026-07-09 [مراجعة شاشة الأرشيف]:** إضافة مراجعة شاشة الأرشيف العام إلى `docs/UI_REVIEW_AND_REDESIGN_PLAN.md`، وتوثيق أن المطابقة الحالية تقارب 45% بسبب غياب البحث الموحد، فلاتر الملفات الناقصة والجلسات القريبة وبانتظار الأساس، ووجود تبويبات Placeholder في الأشخاص والوكالات.
- **2026-07-09 [مراجعة الواجهات قبل التعديل]:** إنشاء وثيقة `docs/UI_REVIEW_AND_REDESIGN_PLAN.md` كملف مرجعي لمراجعة الشاشات شاشة شاشة، وتضمين مراجعة الشاشة الرئيسية الأولى، ونتائج البحث الأولي حول الثيمات العصرية وتوزيع التبويبات والملاحة قبل أي تعديل برمجي جديد.
- **2026-07-08 [إصلاح تشغيل Windows/ICU]:** تصحيح مسار تثبيت ملف `icudtl.dat` ضمن `build/windows/x64/runner/Debug/data/` مباشرة بدل وضعه داخل `data/flutter_assets/`، لأن محرك Flutter يحتاجه في جذر مجلد `data` عند تشغيل `lawyer_office.exe`.
- **2026-07-08 [إصلاح Windows Runner/C++]:** استبدال ملفات `win32_window.h/cpp` بقالب Flutter الرسمي الحديث وتصحيح صلاحية `WndProc` وواجهات نافذة Windows، لمعالجة أخطاء MSVC الخاصة بـ `private member WndProc` و `WindowClassRegistrar ambiguous symbol`.
- **2026-07-08 [إصلاح تفصيلي بعد التجميع]:** تصحيح تمرير `entityType` في `FinanceRepository.addPayment` كقيمة رقمية مباشرة عند تسجيل حدث الدفعة المالية في الخط الزمني، لأن توقيع الدالة يستقبل `int` وليس `EntityType`.
- **2026-07-08 [إصلاح تجميعي شامل لأخطاء Flutter/Drift الحديثة]:** معالجة جذرية للأخطاء التي ظهرت بعد نجاح CMake، عبر إبقاء أعمدة التعدادات في Drift كقيم رقمية متوافقة مع منطق التطبيق الحالي بدل توليد أنواع Enum داخل `database.g.dart`، وإصلاح تعارض `LegalEntities.entityName` مع `TableInfo.entityName` بتسمية Dart آمنة مع الحفاظ على اسم عمود SQLite، وتصحيح نوع المصاريف `Expense`، وتحديث توافق Flutter الحديث (`CardThemeData`) وواجهات PDF (`allowSharing`, `EdgeInsets.only`) وإضافة أرقام مؤقتة عند إنشاء الملفات قبل أن تستبدلها المستودعات بالترقيم السنوي الرسمي.
- **2026-07-08 [إصلاح تشغيل Windows/OpenSSL]:** إزالة اعتماد `sqlcipher_flutter_libs` من نسخة Windows الحالية لأنه يجبر CMake على البحث عن OpenSSL محلي غير موجود على جهاز المستخدم، والاكتفاء بـ `sqlite3_flutter_libs` لتشغيل التطبيق Offline مباشرة دون تثبيت برامج إضافية. تم أيضاً تنظيف شاشة إنشاء العقد من تعبير مسار ملف قديم، وتصحيح حقل سجل النشاط `table_name` في Drift كي يكتمل التوليد دون تحذير بنيوي.
- **2026-07-08 [المرحلة 9]:** إصلاح جذر مشكلة تشغيل Windows عبر إضافة مشروع سطح المكتب الكامل `windows/` إلى المستودع، وضبط CMake والـ runner وملفات التسجيل الأولية للإضافات، وتوثيق الحل بحيث يتعرف Flutter مباشرة على المنصة ولا يظهر خطأ `No Windows desktop project configured` عند تنفيذ `flutter run -d windows`.
- **2026-07-07 [المرحلة 8]:** إتمام التدقيق الشامل والاختبار الهيكلي لكافة ملفات المشروع (61 ملف Dart بنسبة 100% نجاح في التوازن ومطابقة الواجهات واتصال SQLite المحلي)، وإعداد تقرير التدقيق والتسليم الرسمي (`docs/AUDIT_AND_DELIVERY_REPORT.md`)، وتوثيق تطابق الميزات بنسبة 100% مع الدستور وميثاق V6.2.
- **2026-07-07 [المرحلة 7]:** إتمام بناء شاشة إدارة الأعمال اليومية والأجندة (`DailyTasksScreen` مع تصنيف مهام اليوم والمتاخرة والمنجزة وأزرار الإتمام والتأجيل مع طلب السبب والتاريخ الجديد)، شاشة البحث المتقدم الفوري (`AdvancedSearchScreen` في كل ملفات الدعاوى، العقود، الشركات، والإجراءات)، شاشة الطباعة القانونية الرسمية وتصدير تقارير PDF (`LegalPrintingScreen` مع الترويسة السورية الرسمية "مكتب المحامي • هادي فيصل البني" وخطوط Cairo/Amiri)، وشاشة الإعدادات والأمان الموحدة (`SettingsScreen` لإدارة بيانات المكتب والترويسة، فحص الحماية المحلية وتغيير كلمة المرور وسؤال الأمان، تشغيل واستعادة النسخ الاحتياطي في Isolate خلفي مع دعم أقراص USB، وإدارة القوائم السورية للمحاكم والمواضيع).
- **2026-07-07 [المرحلة 6]:** إتمام بناء واجهات تأسيس وإدارة الشركات بدورة الحياة الكاملة (`CreateCompanyWizard` مع توليد المراحل الـ 10 تلقائياً، وإدارة الشركاء وحصص رأس المال، وشاشة الملف الموحد بتبويباته الثمانية)، شاشات تنظيم العقود (`CreateContractScreen` مع ربط التنبيهات المجدولة ومتابعة الانتهاء والتجديد، شاشة `ContractDetailScreen` مع محرر النص الداخلي وزر **"فتح وتحرير في Word 📝"** عبر OpenFilex، وشاشة إدارة القوالب `.docx` الدائمة)، وشاشات الإجراءات الإدارية والمعاملات (`CreateProcedureScreen` مع توليد خطوات الـ Checklist التلقائية وشرط تحديد موعد المراجعة القادم، وشاشة تفاصيل المعاملة بتبويباتها الستة).
- **2026-07-07 [المرحلة 5]:** إتمام بناء نظام إدارة الدعاوى القضائية بالكامل (`CasesListScreen` لأرشيف الدعاوى مع الفلترة والبحث الفوري برقم الأساس والرقم السنوي، معالج إنشاء الدعوى `CreateCaseWizard` مع إلزامية تحديد موعد الجلسة القادمة وتدقيق النواقص وربط الوكالة، وشاشة ملف الدعوى الموحد `CaseDetailScreen` بتبويباته التسعة: الملخص، الأطراف والوكالات، المراحل القضائية مع زر النقل التلقائي للمحكمة الأعلى ونقل القرار والمبرزات، الجلسات وحضور الأطراف، المستندات، المالية المنفصلة لكل موكل، النواقص مع زر الاستكمال الفوري، الخط الزمني الشامل، وشاشة الإنهاء مع فرض إرفاق صورة الحكم النهائي).
- **2026-07-07 [المرحلة 4]:** إتمام بناء واجهات المستخدم للملاحة الرئيسية، لوحة التحكم، شاشة الأرشيف العام، شاشات إدارة الأشخاص وفريق المكتب (مع دعم الفلاتر التبويبية والبحث الفوري والتمييز بين الشخص الطبيعي والاعتباري ورفع سند التمثيل)، وشاشات إدارة الوكالات القضائية والقانونية (إصدار وكالة كاتب عدل أو مندوب نقابة مع القائمة السورية لفروع النقابة الـ 14 وإرفاق السند).
- **2026-07-07 [المرحلة 3]:** إتمام بناء طبقة المستودعات التجريدية الـ 9 (`PersonRepository`, `PoaRepository`, `CaseRepository`, `CompanyRepository`, `ContractRepository`, `AdminProcedureRepository`, `TaskRepository`, `FinanceRepository`, `DocumentRepository`, `LookupRepository`) وإعداد مزودات الحالة في Riverpod (`app_providers.dart`) مع هندسة دالة نقل المراحل القضائية ونقل القرارات والمبرزات أوتوماتيكياً وتوصيل تدفقات البيانات الحية (StreamProviders) لجميع واجهات التطبيق.
- **2026-07-07 [المرحلة 2]:** إتمام بناء محركات المنطق والخدمات الخلفية الـ 5 (`SequenceService` لتوليد الترقيم السنوي الآمن مثل `2026/001` عبر قفل المعاملات، `TaskSyncService` لأتمتة مهام اليوم من الجلسات وتذكيرات العقود ومراحل الشركات وإدارة التأجيل وحفظ الخط الزمني، `DeficiencyService` لمراقبة النواقص القضائية التلقائية وإغلاقها فور تعبئتها، `FileStorageService` لتنظيم مسارات المرفقات ونماذج Word في `AppData/LawOffice/files/`، و `BackupService` للنسخ الاحتياطي الأسبوعي الذكي وضغط `.zip` في Isolate مستقل مع دعم القرص الخارجي والاستعادة).
- **2026-07-07 [المرحلة 1]:** إتمام بناء مخطط قاعدة البيانات في Drift بـ 22 جدولاً مترابطاً (الأشخاص، الكيانات، الوكالات، المحاكم، الدعاوى وجلساتها، الشركات، العقود ونماذج Word، الإجراءات الإدارية، الأعمال اليومية، المالية الموحدة، المستندات، النواقص، والخط الزمني)، وإعداد اتصال SQLite المحلي، وبذور البيانات السورية الابتدائية، وبناء 9 محركات وصول بيانات (DAOs) متخصصة.
- **2026-07-07 [المرحلة 0]:** إتمام تأسيس البنية التحتية للمشروع بالكامل (`pubspec.yaml`, `lib/core/`, `lib/presentation/`)، تهيئة الخطوط والألوان الرسمية والملاحة، وضبط اسم التطبيق "مكتب المحامي" وتحته "هادي فيصل البني" كإعدادات افتراضية قابلة للتعديل ديناميكياً من شاشة الإعدادات.
