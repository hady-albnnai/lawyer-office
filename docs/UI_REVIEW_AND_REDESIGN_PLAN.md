# خطة مراجعة وإعادة تصميم واجهات تطبيق مكتب المحامي

> **التاريخ:** 2026-07-09  
> **الحالة:** مرحلة مراجعة تصميمية ووظيفية قبل أي تعديل برمجي جديد  
> **النطاق الحالي:** شاشة واحدة في كل مرة، بدءاً من الشاشة الرئيسية / تبويب **جديد ➕**  
> **الهدف:** مطابقة ما نُفّذ مع الخطة الأصلية، كشف الانحرافات، ثم إعادة تصميم واجهات التطبيق بشكل عصري ومهني مناسب لبرنامج سطح مكتب قانوني سوري.

---

## 1. منهجية العمل المعتمدة من الآن وصاعداً

لن يتم تعديل أي شاشة مباشرة قبل المرور بالمراحل الآتية:

1. **قراءة الخطة الأصلية للشاشة** من `docs/EXECUTION_PLAN.md`.
2. **قراءة ملفات التنفيذ الفعلية** الخاصة بالشاشة.
3. **مطابقة بند ببند** بين المطلوب والمنفذ.
4. **تصنيف الأخطاء** إلى:
   - أخطاء وظيفية تمنع سير العمل.
   - أخطاء معمارية تخالف منطق التطبيق.
   - أخطاء تجربة مستخدم وتصميم.
   - أخطاء نصوص، Lookups، أو hardcoded values.
5. **اقتراح تصميم عصري بديل** قبل البرمجة.
6. **توثيق قرار التعديل ومعايير القبول**.
7. بعد موافقة الأستاذ هادي: تنفيذ التعديل، اختبار البناء، تحديث التوثيق، ثم الرفع على Git.

---

## 2. ملخص البحث التصميمي: الثيمات العصرية المناسبة

### 2.1 الاتجاهات العامة المناسبة للتطبيق

من مراجعة اتجاهات تصميم واجهات 2026 وتطبيقات سطح المكتب الحديثة، الأنسب لتطبيق مكتب محاماة ليس التصميم الاستعراضي، بل تصميم مهني يعتمد على:

- **Minimal Professional UI:** واجهة نظيفة، فراغات واضحة، ألوان قليلة، وتباين قراءة ممتاز.
- **Human-Centric Dashboard:** لوحة قيادة تركز على ما يحتاجه المحامي اليوم: جلسات، مهام، نواقص، ملفات حرجة.
- **Light-first Theme:** لأن التطبيق مكتبي، نصي، قانوني، ويحتاج طباعة وتدقيق طويل؛ الوضع الفاتح أنسب كافتراضي.
- **Optional Dark Mode لاحقاً:** مفيد للعمل الليلي، لكن لا يكون الافتراضي الأول.
- **Bento Grid / Modular Cards:** بطاقات منظمة للملخصات والإجراءات السريعة بدل شبكة عشوائية.
- **Fluent/Mica-like surfaces:** استخدام طبقات خفيفة، ظلال ناعمة، وحدود subtle وليس ألوان داكنة ثقيلة في كل مكان.

### 2.2 الثيم المقترح لتطبيق مكتب المحامي

اسم الثيم المقترح:

> **Syrian Legal Modern Light**

#### الألوان المقترحة

- **Primary Navy مخفف:** `#102A43` أو `#123047` بدل الكحلي شبه الأسود الحالي.
- **Legal Gold هادئ:** `#B9975B` للاختيار، الأيقونات المهمة، وخط الأستاذ.
- **Background:** `#F6F8FA` أو `#F7F8FA`.
- **Surface:** أبيض مع حدود خفيفة `#E5E7EB`.
- **Danger:** أحمر واضح للنواقص فقط، وليس للزينة.
- **Warning:** كهرماني للمواعيد القريبة.
- **Success:** أخضر هادئ للمنجز.

#### ملاحظات على الثيم الحالي

الثيم الحالي فيه نواة جيدة، لكنه يحتاج:

- تخفيف قتامة شريط الملاحة.
- توحيد الظلال والحدود.
- إضافة typography scale واضح.
- تضمين خطوط Cairo/Amiri فعلياً في `pubspec.yaml` بدل ذكرها فقط.
- دعم density مناسب لبرنامج سطح مكتب، لأن واجهات المحاماة تحتاج عرض بيانات كثيرة بدون ازدحام.

---

## 3. ملخص البحث التصميمي: توزيع التبويبات والملاحة

### 3.1 المبادئ المهنية للملاحة في Desktop

حسب إرشادات Material 3، تطبيقات سطح المكتب لا يُفضّل لها Bottom Navigation، بل الأفضل استخدام:

- **Navigation Rail** للشاشات المتوسطة والكبيرة.
- **Expanded Navigation Rail / Navigation Drawer** للشاشات الواسعة.
- **Tabs داخل الصفحات** فقط للمحتوى الفرعي، وليس للتنقل الرئيسي.

### 3.2 التوزيع المقترح للتطبيق

التوزيع الحالي فيه 6 تبويبات، لكنه يخلط بين “فريق المكتب” و “الأشخاص” وبين “جديد” كصفحة مستقلة. المقترح:

#### الملاحة الرئيسية

1. **لوحة اليوم**
   - مهام اليوم
   - جلسات قريبة
   - ملفات ناقصة
   - أعمال متأخرة

2. **عمل جديد**
   - زر/معالج منظم وليس Grid فقط
   - يبدأ بسؤال: بدء عمل جديد أم أرشفة عمل سابق؟

3. **الأرشيف**
   - دعاوى
   - عقود
   - شركات
   - إجراءات إدارية
   - وكالات
   - مستندات

4. **الأشخاص والجهات**
   - موكلون
   - خصوم
   - محامو خصم
   - كتاب عدل ومندوبون
   - فريق المكتب

5. **البحث والتقارير**
   - بحث شامل
   - طباعة قانونية
   - تصدير PDF

6. **الإعدادات والأمان**
   - بيانات المكتب
   - النسخ الاحتياطي
   - القوائم المرجعية
   - الأمان

### 3.3 مبدأ التبويبات الداخلية

أي شاشة ملف، مثل دعوى أو شركة أو عقد، تستخدم Tabs داخلية. أما الشاشة الرئيسية فتستخدم NavigationRail أو Sidebar فقط.

---

## 4. مراجعة الشاشة الأولى: الشاشة الرئيسية / تبويب جديد

### 4.1 الملفات التي تمت مراجعتها

- `lib/main.dart`
- `lib/app.dart`
- `lib/presentation/navigation/app_router.dart`
- `lib/presentation/screens/main_layout_screen.dart`
- `lib/presentation/providers/office_settings_provider.dart`
- `lib/core/constants/app_constants.dart`
- `lib/core/theme/app_theme.dart`

### 4.2 المطلوب بالخطة الأصلية

من المرحلة 4.1:

- شريط ملاحة عربي يحوي التبويبات الستة الأساسية.
- نافذة **عمل جديد** منبثقة لاختيار:
  - بدء عمل جديد.
  - أرشفة عمل سابق.
  - ثم تحديد التصنيف.

من المرحلة 0.3:

- دعم RTL كامل.
- الثيم الرسمي.
- اسم المكتب والأستاذ كإعدادات قابلة للتعديل.

### 4.3 المنفذ فعلياً

- يوجد `NavigationRail` بستة تبويبات.
- يوجد AppBar يعرض اسم المكتب واسم الأستاذ.
- تبويب جديد يعرض أربع بطاقات:
  - دعوى قضائية.
  - إجراء إداري.
  - تأسيس شركة.
  - تنظيم عقد.
- يوجد زر طباعة وزر تنبيهات في AppBar.
- الإعدادات محفوظة عبر `SharedPreferences`.

### 4.4 الانحرافات والأخطاء المكتشفة

| رقم | الخطأ | التصنيف | الخطورة | الملاحظة |
|---|---|---|---|---|
| 1 | نافذة “عمل جديد” غير موجودة | وظيفي/UX | عالية | المطلوب معالج يبدأ بسؤال جديد/أرشفة سابق. |
| 2 | مسار “أرشفة عمل سابق” غير موجود | وظيفي | عالية | يخالف نص الخطة مباشرة. |
| 3 | التصنيفات ناقصة | وظيفي | متوسطة | لا توجد وكالة، شخص، مستند مستقل، مهمة يدوية. |
| 4 | GoRouter موجود شكلياً فقط | معماري | متوسطة | التنقل يتم بـ `MaterialPageRoute`. |
| 5 | زر التنبيهات غير فعلي | UX/وظيفي | متوسطة | ينقل للأعمال اليومية فقط بلا عداد أو قائمة. |
| 6 | فريق المكتب يفتح شاشة الأشخاص العامة | UX | متوسطة | يحتاج تصنيف أو اسم أدق. |
| 7 | إعدادات المكتب في SharedPreferences بدل AppSettings | معماري | عالية | يخالف مركزية قاعدة البيانات. |
| 8 | نصوص hardcoded كثيرة | جودة/قابلية صيانة | متوسطة | الأفضل ثوابت/Lookups. |
| 9 | الخطوط غير مضمنة فعلياً | تصميم | متوسطة | Cairo/Amiri مذكورة لكن غير معرفة كأصول. |
| 10 | Grid ثابت بأربع أعمدة | Responsive UX | متوسطة | يحتاج LayoutBuilder. |
| 11 | كود غير مستخدم `_buildSettingsPreview` | جودة | منخفضة | يجب حذفه أو نقله. |

### 4.5 تقييم المطابقة الحالي

**المطابقة الحالية:** 55% تقريباً.

الشاشة تعمل، لكنها لا تحقق منطق “عمل جديد” كما هو مطلوب، ولا تعطي واجهة عصرية كافية لبرنامج سطح مكتب قانوني.

---

## 5. خطة تعديل الشاشة الأولى بعد الموافقة

### 5.1 الهدف من التعديل

تحويل الشاشة الرئيسية من Grid بدائي إلى لوحة عمل قانونية احترافية فيها:

- لوحة يومية مفيدة.
- معالج عمل جديد واضح.
- أرشفة عمل سابق.
- ملاحة أكثر عصرية.
- أسلوب بصري مهني.

### 5.2 التصميم المقترح للشاشة

#### الهيكل العام

- **Sidebar/NavigationRail قابل للتوسعة** على اليمين بسبب RTL.
- **Header أعلى المحتوى** يحوي:
  - اسم المكتب.
  - اسم الأستاذ.
  - التاريخ الحالي.
  - زر بحث سريع.
  - زر إشعارات مع عداد.
- **منطقة محتوى رئيسية** بتصميم Bento Cards.

#### محتوى تبويب “عمل جديد”

بدل البطاقات الأربع المباشرة، يظهر:

1. بطاقة كبيرة: **بدء عمل جديد**
2. بطاقة كبيرة: **أرشفة عمل سابق**

بعد الضغط تظهر نافذة اختيار التصنيف:

- دعوى قضائية.
- إجراء إداري.
- شركة.
- عقد.
- وكالة.
- شخص/جهة.
- مستند مستقل.
- مهمة يدوية.

### 5.3 معايير القبول

لا يعتبر تعديل الشاشة مقبولاً إلا إذا تحقق التالي:

- وجود خيارين واضحين: بدء جديد / أرشفة سابق.
- فتح نافذة أو معالج تصنيف واضح.
- دعم RTL دون التفاف أو اختلال.
- عدم وجود نصوص رئيسية مشتتة داخل الكود قدر الإمكان.
- استخدام GoRouter أو توثيق سبب مؤقت لعدم التحويل الكامل.
- Grid متجاوب حسب عرض النافذة.
- وجود مؤشرات على الأقل لـ:
  - مهام اليوم.
  - الملفات الناقصة.
  - الجلسات القريبة.
  - المتأخرات.
- عدم كسر تشغيل Windows.

### 5.4 ما لن يتم تعديله في أول خطوة

حتى لا نخلط المهام، أول تعديل لن يدخل في:

- إعادة بناء كل شاشات الأرشيف.
- تعديل قاعدة البيانات.
- تعديل ملفات الدعوى أو الشركة أو العقد.
- تنفيذ الأمان الكامل.

هذه تبقى لمراحل لاحقة شاشة شاشة.

---

## 6. خطة مراجعة باقي الشاشات

بعد اعتماد الشاشة الأولى، يتم الانتقال بهذا الترتيب:

1. الشاشة الرئيسية / عمل جديد.
2. الأرشيف.
3. الأشخاص والجهات.
4. الوكالات.
5. معالج إنشاء الدعوى.
6. ملف الدعوى.
7. الشركات.
8. العقود.
9. الإجراءات الإدارية.
10. الأعمال اليومية.
11. البحث والتقارير.
12. الإعدادات والأمان.

---

## 7. قرار المرحلة الحالية

حالياً لا يوجد تعديل برمجي مطلوب.  
هذه الوثيقة هي ملف مرجعي قبل بدء إعادة التصميم.

الخطوة التالية بعد موافقة الأستاذ هادي: تنفيذ تعديل الشاشة الأولى وفق هذه الخطة، ثم تحديث هذه الوثيقة بنتيجة التنفيذ.

---

## 8. مراجعة الشاشة الثانية: الأرشيف العام

### 8.1 الملفات التي تمت مراجعتها

- `lib/presentation/screens/archive/archive_screen.dart`
- `lib/presentation/screens/cases/cases_list_screen.dart`
- `lib/presentation/screens/companies/companies_list_screen.dart`
- `lib/presentation/screens/contracts/contracts_list_screen.dart`
- `lib/presentation/screens/admin_procedures/procedures_list_screen.dart`
- `lib/presentation/screens/persons/persons_list_screen.dart`
- `lib/presentation/screens/poa/poa_list_screen.dart`
- `lib/presentation/providers/app_providers.dart`

### 8.2 المطلوب بالخطة الأصلية

وفق الخطة الأصلية، شاشة الأرشيف يجب ألا تكون مجرد تبويبات منفصلة، بل مركز أرشفة وتشغيل يسمح بالوصول السريع والبحث والفلترة في ملفات المكتب، وخاصة:

- عرض ملفات الدعاوى والعقود والشركات والإجراءات والأشخاص والوكالات.
- البحث الفوري والشامل.
- فلاتر مخصصة: ملفات اليوم، ملفات ناقصة، جلسة قريبة، بانتظار استكمال رقم الأساس، ملفات منتهية.
- إظهار الحالة التشغيلية والقانونية لكل ملف.
- دعم الوصول السريع للملف وتفاصيله.

### 8.3 المنفذ فعلياً

- `ArchiveScreen` ينفذ `TabBar` بستة تبويبات: الدعاوى، الإجراءات، الشركات، العقود، الأشخاص، الوكالات.
- كل تبويب يفتح شاشة قائمة مستقلة.
- يوجد بحث محلي داخل بعض الأقسام، مثل الدعاوى والعقود والشركات والإجراءات والأشخاص.
- لا يوجد بحث موحد على مستوى الأرشيف كله.
- لا توجد فلاتر تشغيلية موحدة على مستوى الأرشيف.

### 8.4 نسبة المطابقة الحالية

**45% تقريباً.**

الهيكل العام موجود، لكن منطق الأرشيف الذكي والمركزي غير منفذ.

### 8.5 الانحرافات والأخطاء المكتشفة

| رقم | الخطأ | التصنيف | الخطورة | الملاحظة |
|---|---|---|---|---|
| 1 | الأرشيف غير موحد فعلياً | وظيفي/UX | عالية | هو مجرد تجميع تبويبات لقوائم منفصلة. |
| 2 | فلاتر الخطة الأساسية غير موجودة | وظيفي | عالية | لا توجد ملفات اليوم، ناقصة، جلسة قريبة، بانتظار الأساس، منتهية. |
| 3 | لا يوجد فلتر الملفات الناقصة | وظيفي | عالية جداً | رغم وجود `deficiencies` و `openDeficienciesProvider`. |
| 4 | لا يوجد فلتر جلسة قريبة أو متأخرة | وظيفي | عالية | مهم جداً للعمل اليومي للمحامي. |
| 5 | لا يوجد فلتر بانتظار رقم الأساس | وظيفي | متوسطة/عالية | يظهر النص داخل البطاقة فقط دون فلترة. |
| 6 | لا يوجد فصل واضح بين ملف جديد وملف سابق مؤرشف | منطق عمل | عالية | مرتبط بمسار “أرشفة عمل سابق” غير المنفذ في الشاشة الرئيسية. |
| 7 | تبويبات الأشخاص لا تفلتر فعلياً حسب الدور | وظيفي | عالية جداً | `filterRole` موجود لكنه غير مستخدم في `_buildPersonsList`. |
| 8 | محامو الأخصام وكتاب العدل وفريق المكتب Placeholder | وظيفي | عالية | ليست قوائم فعلية. |
| 9 | الوكالات بلا بحث وبلا فلاتر وبلا شاشة تفاصيل | وظيفي/UX | عالية | الضغط يعرض SnackBar فقط. |
| 10 | وجود `Scaffold` داخل تبويبات الأرشيف | UI/معماري | متوسطة | يسبب تداخل AppBars وتجربة غير موحدة. |
| 11 | التنقل ما زال بـ `MaterialPageRoute` | معماري | متوسطة | يخالف اعتماد GoRouter كملاحة موحدة. |
| 12 | العقود لا تعرض انتهاء/تجديد/تذكير قريب بوضوح | وظيفي | متوسطة/عالية | منطق العقود التشغيلي غير ظاهر في الأرشيف. |
| 13 | الإجراءات لا تعرض الحالة والموعد القادم وآخر خطوة | وظيفي | متوسطة/عالية | لا تساعد في متابعة المعاملة. |
| 14 | الشركات لا تعرض المرحلة الحالية أو النواقص | وظيفي | متوسطة | الأرشيف لا يكشف تعثر التأسيس. |
| 15 | لا توجد إحصائيات أعلى الأرشيف | UX | متوسطة | الأرشيف لا يقدم لوحة قيادة. |
| 16 | نصوص وتصنيفات hardcoded | جودة | متوسطة | الأفضل ثوابت/Lookups مركزية. |

### 8.6 قرار المراجعة

شاشة الأرشيف تعمل كقوائم أساسية، لكنها لا تطابق مفهوم الأرشيف التشغيلي المطلوب. عند التعديل لاحقاً يجب تحويلها إلى شاشة أرشيف موحدة ذكية.

### 8.7 خطة تعديل الأرشيف لاحقاً

عند الوصول لمرحلة التعديل، يجب تنفيذ ما يلي:

1. بناء شريط بحث موحد يبحث في الدعاوى والعقود والشركات والإجراءات والأشخاص والوكالات.
2. إضافة فلاتر تشغيلية واضحة:
   - الكل.
   - ملفات اليوم.
   - الملفات الناقصة.
   - جلسات قريبة.
   - بانتظار رقم الأساس.
   - ملفات منتهية.
   - متأخرات.
3. إضافة بطاقات إحصائية أعلى الشاشة:
   - عدد الملفات العاملة.
   - عدد النواقص.
   - جلسات الأسبوع.
   - عقود تنتهي قريباً.
   - شركات قيد التأسيس.
   - معاملات متأخرة.
4. إصلاح فلاتر الأشخاص حسب الأدوار الحقيقية من قاعدة البيانات.
5. تحويل تبويبات محامي الأخصام وكتاب العدل وفريق المكتب من placeholders إلى قوائم فعلية.
6. إضافة بحث وفلاتر وشاشة تفاصيل للوكالات.
7. إزالة الـ `Scaffold` المتداخلة داخل `TabBarView` وتوحيد التصميم.
8. اعتماد GoRouter أو توثيق خطة التحويل التدريجي له.
9. نقل التصنيفات المتكررة إلى ثوابت أو Lookups.

### 8.8 معايير قبول تعديل الأرشيف مستقبلاً

- ظهور نتائج بحث موحدة من أكثر من نوع ملف.
- عمل فلتر الملفات الناقصة اعتماداً على جدول `deficiencies`.
- عمل فلتر الجلسات القريبة اعتماداً على `nextSessionDate` والمهام اليومية.
- عمل فلتر بانتظار الأساس اعتماداً على `baseNumber`.
- فتح تفاصيل حقيقية لكل نتيجة.
- عدم وجود تبويبات Placeholder.
- عدم وجود AppBars داخلية متداخلة ضمن الأرشيف.


---

## 9. مراجعة الشاشة الثالثة: الأشخاص والجهات / فريق المكتب

### 9.1 الملفات التي تمت مراجعتها

- `lib/presentation/screens/persons/persons_list_screen.dart`
- `lib/presentation/screens/persons/add_person_dialog.dart`
- `lib/presentation/screens/persons/person_detail_screen.dart`
- `lib/data/repositories/person_repository.dart`
- `lib/data/database/daos/person_dao.dart`
- `lib/data/database/schema.dart`

### 9.2 المطلوب بالخطة الأصلية

وفق المرحلة 4.2، شاشة الأشخاص وفريق المكتب يجب أن تتضمن:

- قائمة شاملة بفلاتر واضحة: موكلون، خصوم، محامو خصم، كتاب عدل ومندوبون، فريق المكتب.
- تمييز الشخص الطبيعي عن الاعتباري.
- شاشة تفاصيل موكل تعرض بياناته وقضاياه وعقوده وشركاته المرتبطة.
- إدارة فريق المكتب: محامي أستاذ، متمرن، معقب، موظفة مكتب.
- دليل محامي الأخصام وكتاب العدل ومندوبي فروع النقابة الـ 14.
- ربط الأشخاص بالوكالات والدعاوى والعقود والشركات.

### 9.3 المنفذ فعلياً

- توجد شاشة `PersonsListScreen` بتبويبات ستة.
- يوجد حوار إضافة شخص/جهة اعتبارية `AddPersonDialog`.
- يوجد جدول `PersonRoles` و `TeamMembers` و `OpponentLawyers` و `Notaries` في قاعدة البيانات.
- يوجد `PersonDetailScreen`، لكنه غير موصول فعلياً من القائمة، وتبويباته المرتبطة أغلبها placeholders.
- توجد DAOs و Repository أولية للأشخاص والأدوار والفريق وكتاب العدل.

### 9.4 نسبة المطابقة الحالية

**40% تقريباً.**

الشاشة تبدو مكتملة بصرياً، لكن جوهر الفلترة والربط وتفاصيل الشخص غير مكتمل.

### 9.5 الانحرافات والأخطاء المكتشفة

| رقم | الخطأ | التصنيف | الخطورة | الملاحظة |
|---|---|---|---|---|
| 1 | تبويبات الموكلين والخصوم لا تفلتر فعلياً | وظيفي | عالية جداً | `filterRole` لا يُستخدم، وكل الأشخاص يظهرون في التبويبات. |
| 2 | الضغط على الشخص لا يفتح `PersonDetailScreen` | وظيفي | عالية | يعرض SnackBar فقط. |
| 3 | شاشة تفاصيل الشخص تحتوي placeholders | وظيفي | عالية | القضايا والعقود والشركات والخط الزمني غير موصولة. |
| 4 | محامو الأخصام Placeholder | وظيفي | عالية | رغم وجود جدول وDAO. |
| 5 | كتاب العدل والمندوبون Placeholder | وظيفي | عالية | رغم وجود بذور وDAO. |
| 6 | فريق المكتب Placeholder | وظيفي | عالية | لا يعرض أعضاء الفريق ولا يسمح بإدارتهم. |
| 7 | إضافة الشخص لا تنشئ TeamMember عند اختيار دور فريق المكتب | منطق بيانات | عالية | يتم فقط إدخال PersonRole. |
| 8 | إضافة محامي خصم/كاتب عدل غير مخصصة | UX/وظيفي | متوسطة/عالية | نفس حوار الأشخاص لا يكفي لكل نوع. |
| 9 | بيانات الشخص الاعتباري لا تظهر كاملة في التفاصيل | وظيفي | متوسطة | لا يتم جلب `LegalEntity` في `PersonDetailScreen`. |
| 10 | لا توجد شاشة تعديل حقيقية | وظيفي | متوسطة | زر التعديل يعرض SnackBar فقط. |
| 11 | لا يوجد تحقق من تكرار الهوية أو الهاتف | جودة بيانات | متوسطة | قد يؤدي إلى سجلات مكررة. |
| 12 | نصوص وتصنيفات hardcoded | جودة | متوسطة | الأدوار والتسميات تحتاج ثوابت/Lookups. |
| 13 | استخدام helper محلي باسم `p` بدل `package:path` | جودة | منخفضة/متوسطة | حل مؤقت وغير مهني في واجهة مهمة. |
| 14 | لا يوجد سجل نشاط عند إنشاء/تعديل شخص | معماري | متوسطة | يخالف متطلبات ActivityLog. |
| 15 | لا يوجد ربط مباشر من الشخص إلى الوكالات | وظيفي | متوسطة/عالية | تفاصيل الشخص لا تعرض وكالاته رغم مركزية الأشخاص. |

### 9.6 قرار المراجعة

شاشة الأشخاص والجهات هي من أكثر الشاشات التي تبدو مكتملة ظاهرياً لكنها ناقصة وظيفياً. الخلل الأساسي هو أن طبقة البيانات موجودة جزئياً، لكن الواجهة لا تستخدمها بالشكل المطلوب.

### 9.7 خطة تعديل الشاشة لاحقاً

عند الوصول لمرحلة التعديل، يجب تنفيذ ما يلي:

1. إصلاح فلترة الموكلين والخصوم اعتماداً على `PersonRoles` وليس على كل الأشخاص.
2. ربط `onTap` بفتح `PersonDetailScreen` فعلياً.
3. توصيل تبويبات تفاصيل الشخص:
   - الدعاوى المرتبطة.
   - العقود المرتبطة.
   - الشركات والحصص.
   - الوكالات.
   - الخط الزمني.
4. بناء قوائم فعلية لمحامي الأخصام وكتاب العدل/المندوبين وفريق المكتب.
5. إنشاء حوارات إضافة/تعديل مخصصة لكل نوع عند الحاجة.
6. عند اختيار دور فريق المكتب، إنشاء سجل `TeamMembers` مناسب.
7. عرض بيانات الشخص الاعتباري من `LegalEntities` داخل التفاصيل.
8. إضافة تحقق من التكرار للهوية والهاتف والسجل التجاري.
9. نقل النصوص والتصنيفات المتكررة إلى ثوابت أو Lookups.
10. تسجيل الحركات المهمة في `ActivityLog`.

### 9.8 معايير قبول تعديل الشاشة مستقبلاً

- تبويب الموكلين يعرض الموكلين فقط.
- تبويب الخصوم يعرض الخصوم فقط.
- تبويب فريق المكتب يعرض أعضاء الفريق الفعليين.
- تبويب محامي الأخصام يعرض سجلات `OpponentLawyers`.
- تبويب كتاب العدل يعرض سجلات `Notaries` مع فروع النقابة.
- فتح الشخص يعرض تفاصيل حقيقية وروابطه بالدعاوى والعقود والشركات والوكالات.
- لا توجد شاشات أو تبويبات Placeholder.


---

## 10. مراجعة الشاشة الرابعة: الوكالات القضائية والقانونية

### 10.1 الملفات التي تمت مراجعتها

- `lib/presentation/screens/poa/poa_list_screen.dart`
- `lib/presentation/screens/poa/add_poa_dialog.dart`
- `lib/data/repositories/poa_repository.dart`
- `lib/data/database/daos/person_dao.dart`
- `lib/data/services/file_storage_service.dart`
- `lib/data/database/schema.dart`

### 10.2 المطلوب بالخطة الأصلية

وفق المرحلة 4.3، شاشة الوكالات يجب أن تتضمن:

- إنشاء وكالة جديدة.
- اختيار مصدر الوكالة: كاتب عدل أو مندوب نقابة.
- اختيار فرع النقابة من القائمة السورية للفروع الـ 14.
- اختيار نوع الوكالة: عامة، خاصة، خاصة شرعية.
- إدخال وصف نطاق الوكالة الخاصة.
- إرفاق صورة سند الوكالة.
- ربط الموكل والوكيل.
- ربط الوكالة بملفات الدعاوى عند الحاجة.
- عرض أرشيف الوكالات والتمييز بين الوكالات الناقصة والمكتملة.

### 10.3 المنفذ فعلياً

- توجد شاشة `PoaListScreen` تعرض الوكالات في قائمة.
- توجد استمارة `AddPoaDialog` لإنشاء وكالة جديدة.
- يوجد اختيار كاتب عدل / مندوب نقابة.
- توجد قائمة فروع النقابة الـ 14.
- يوجد اختيار نوع الوكالة ووصف الخاصة.
- يوجد إرفاق ملف وحفظه في `powers_of_attorney/{id}`.
- يتم ربط الموكل والوكيل في جدول `PoaParties`.
- يوجد Repository وDAO أساسيان.

### 10.4 نسبة المطابقة الحالية

**55% تقريباً.**

إنشاء الوكالة الأساسي موجود، لكن شاشة الأرشيف والتفاصيل والربط القضائي ناقصة.

### 10.5 الانحرافات والأخطاء المكتشفة

| رقم | الخطأ | التصنيف | الخطورة | الملاحظة |
|---|---|---|---|---|
| 1 | لا توجد شاشة تفاصيل وكالة | وظيفي | عالية | الضغط على الوكالة يعرض SnackBar فقط. |
| 2 | لا يوجد بحث في أرشيف الوكالات | UX/وظيفي | عالية | لا يمكن البحث برقم الوكالة أو الموكل أو الفرع. |
| 3 | لا توجد فلاتر للوكالات | UX/وظيفي | عالية | لا فلترة حسب عامة/خاصة/شرعية أو كاتب عدل/مندوب أو ناقصة الصورة. |
| 4 | لا يتم عرض أطراف الوكالة في القائمة | وظيفي | عالية | لا يظهر اسم الموكل أو الوكيل. |
| 5 | لا يتم عرض القضايا المرتبطة بالوكالة | وظيفي | عالية | رغم وجود `CasePoaLinks`. |
| 6 | لا توجد واجهة ربط الوكالة بدعوى من شاشة الوكالات | وظيفي | متوسطة/عالية | يوجد Repository لكن لا يوجد UI واضح. |
| 7 | عدم ربط `notaryId` أو `delegateId` بسجل فعلي | منطق بيانات | عالية | يتم حفظ `delegateBranch` فقط، ولا يتم اختيار Notary/Delegate من جدول `Notaries`. |
| 8 | لا يوجد تحقق من تكرار رقم الوكالة | جودة بيانات | متوسطة | قد تتكرر الوكالات لنفس الفرع أو المصدر. |
| 9 | لا توجد حالة انتهاء/إلغاء/عزل توكيل في الواجهة | وظيفي | عالية | الجدول يحتوي `status` و `expiryDate` لكن لا تُستخدم. |
| 10 | لا يوجد فتح فعلي للمرفق | وظيفي | متوسطة | القائمة تقول صورة مرفقة لكن لا تفتح الملف. |
| 11 | لا يوجد تسجيل ActivityLog أو Timeline عند إنشاء الوكالة | معماري | متوسطة | الحركة المهمة غير مؤرشفة زمنياً. |
| 12 | مصدر الوكالة hardcoded بقيم نصية | جودة | متوسطة | الأفضل ثوابت/Lookups موحدة. |
| 13 | استخدام `Scaffold` داخل تبويب الأرشيف | UI | متوسطة | يسبب عدم اتساق مع هيكل الأرشيف العام. |
| 14 | لا يوجد إضافة سريع لشخص إذا لم يكن الموكل موجوداً | UX | متوسطة | المستخدم مضطر للخروج وإضافة الشخص من شاشة أخرى. |

### 10.6 قرار المراجعة

شاشة الوكالات أفضل من بعض الشاشات السابقة من ناحية إدخال البيانات الأساسية، لكنها لا تزال ناقصة كأرشيف قانوني؛ إذ لا توجد تفاصيل وكالة، ولا عرض أطراف، ولا ربط فعلي ظاهر مع الدعاوى، ولا إدارة دورة حياة الوكالة.

### 10.7 خطة تعديل الشاشة لاحقاً

عند الوصول لمرحلة التعديل، يجب تنفيذ ما يلي:

1. بناء `PoaDetailScreen` يعرض:
   - بيانات الوكالة.
   - الموكلين والوكلاء.
   - صورة السند وفتحها.
   - القضايا المرتبطة.
   - الحالة: فعالة، منتهية، ملغاة، معتزل عنها.
   - الخط الزمني.
2. إضافة بحث في قائمة الوكالات.
3. إضافة فلاتر:
   - عامة / خاصة / شرعية.
   - كاتب عدل / مندوب نقابة.
   - ناقصة صورة.
   - منتهية أو ملغاة.
4. استخدام جدول `Notaries` لاختيار الكاتب/المندوب بدل حفظ الفرع فقط.
5. عرض أسماء أطراف الوكالة في البطاقة.
6. إضافة ربط وكالة بدعوى من شاشة التفاصيل.
7. إضافة فتح المرفق عبر `open_filex`.
8. إضافة تحقق من تكرار رقم الوكالة ضمن المصدر والفرع.
9. تسجيل إنشاء الوكالة وتعديلها وربطها بدعوى في `TimelineEvents` أو `ActivityLog`.
10. إزالة الـ `Scaffold` الداخلي عند دمجها بالأرشيف الجديد.

### 10.8 معايير قبول تعديل الشاشة مستقبلاً

- البحث يعمل برقم الوكالة واسم الموكل والفرع.
- الفلاتر تعمل حسب النوع والمصدر وحالة المرفق.
- كل وكالة تفتح شاشة تفاصيل حقيقية.
- تظهر أسماء الموكل والوكيل في القائمة والتفاصيل.
- يمكن فتح صورة السند.
- يمكن ربط الوكالة بدعوى.
- لا تبقى أي عملية على شكل SnackBar وهمي.


---

## 11. مراجعة الشاشة الخامسة: معالج إنشاء الدعوى القضائية

### 11.1 الملفات التي تمت مراجعتها

- `lib/presentation/screens/cases/create_case_wizard.dart`
- `lib/data/repositories/case_repository.dart`
- `lib/data/database/daos/case_dao.dart`
- `lib/data/services/sequence_service.dart`
- `lib/data/services/task_sync_service.dart`
- `lib/data/services/deficiency_service.dart`
- `lib/data/database/schema.dart`

### 11.2 المطلوب بالخطة الأصلية

وفق المرحلة 5.1، معالج إنشاء الدعوى يجب أن يتضمن تسلسل خطوات إلزامي واضح:

1. الموكل.
2. الوكالة.
3. التصنيف.
4. البيانات الأساسية.
5. الموضوع والطلبات.
6. الخصم.
7. المرفقات.
8. تحديد موعد التنفيذ أو الجلسة القادمة.

كما يجب أن يدعم:

- ربط الوكالة بالدعوى.
- تدقيق النواقص تلقائياً.
- إنشاء مهمة يومية عند تحديد موعد قادم.
- الترقيم السنوي الآمن.
- كشف غياب الموعد أو رقم الأساس أو سند التوكيل.
- التعامل مع الشخص الاعتباري وسند التمثيل.

### 11.3 المنفذ فعلياً

- يوجد `CreateCaseWizard` باستخدام `Stepper`.
- المعالج يحتوي 6 خطوات فقط:
  1. الموكل الرئيسي.
  2. سند التوكيل.
  3. التصنيف والمحكمة.
  4. موضوع الدعوى والطلبات.
  5. الطرف الخصم.
  6. موعد الجلسة / التنفيذ القادم.
- يتم توليد الرقم السنوي في `SequenceService`.
- يتم إنشاء الدعوى والمرحلة الأولى والأطراف وربط الوكالة إن وجدت.
- يتم إنشاء نواقص في حال غياب الموعد أو رقم الأساس أو الوكالة.

### 11.4 نسبة المطابقة الحالية

**50% تقريباً.**

المعالج موجود ويُدخل دعوى، لكن التسلسل ناقص، والمرفقات غير موجودة، وهناك خلل مهم في توليد مهمة اليوم عند إدخال الموعد القادم.

### 11.5 الانحرافات والأخطاء المكتشفة

| رقم | الخطأ | التصنيف | الخطورة | الملاحظة |
|---|---|---|---|---|
| 1 | عدد خطوات المعالج 6 بدل 8 | وظيفي/UX | عالية | خطوة المرفقات وخطوة البيانات الأساسية مفصولتين غير موجودتين كما بالخطة. |
| 2 | لا توجد خطوة مرفقات | وظيفي | عالية جداً | لا يمكن إرفاق لائحة الدعوى أو الوثائق الأولية عند الإنشاء. |
| 3 | الموكلون غير مفلترين حسب دور موكل | جودة بيانات | عالية | القائمة تعرض كل الأشخاص. |
| 4 | الخصوم غير مفلترين حسب دور خصم | جودة بيانات | عالية | القائمة تعرض كل الأشخاص. |
| 5 | الوكالات غير مفلترة حسب الموكل المختار | منطق قانوني | عالية جداً | يمكن اختيار وكالة لا تخص الموكل. |
| 6 | لا توجد إضافة موكل/خصم/وكالة من داخل المعالج | UX | متوسطة/عالية | المستخدم مضطر للخروج للشاشات الأخرى. |
| 7 | لا يوجد اختيار محامي الخصم | وظيفي | متوسطة | رغم وجود جدول `OpponentLawyers`. |
| 8 | التصنيفات hardcoded | جودة | متوسطة | `_caseTypes` و `_subTypes` ثابتة داخل الشاشة بدلاً من Lookups. |
| 9 | المحاكم لا تُفلتر حسب نوع الدعوى أو المرحلة | منطق/UX | متوسطة | `activeCourtsProvider(null)` يجلب كل المحاكم. |
| 10 | الموضوع لا يستخدم `CaseSubjects` | جودة/UX | متوسطة | المستخدم يكتب نصاً حراً فقط. |
| 11 | الموعد القادم لا يولد مهمة يومية فعلياً | وظيفي خطير | عالية جداً | المستودع يمرر `sessionDate` فقط، بينما `TaskSyncService` يشترط `nextSessionDate` لإنشاء `daily_tasks`. |
| 12 | قيمة `nextAction` لا تُحفظ عند إنشاء أول موعد | وظيفي | عالية | `_nextActionController` موجود لكن لا يصل إلى `CaseSessionsCompanion`. |
| 13 | عدم فحص سند تمثيل الشخص الاعتباري | نواقص | عالية | `hasRepresentativeDoc` يمرر دائماً `false` دون ربطه بحالة الموكل. |
| 14 | لا توجد واجهة أرشفة دعوى سابقة | منطق عمل | عالية | المعالج فقط لفتح دعوى جديدة. |
| 15 | لا يوجد GoRouter | معماري | متوسطة | الانتقال بعد الإنشاء يتم بـ `MaterialPageRoute`. |
| 16 | إدخال `internalNumber` مؤقت في الواجهة | معماري | متوسطة | أُضيف لتوافق البناء، لكن الأفضل توفير مسار إنشاء لا يحتاجه من الواجهة. |
| 17 | لا توجد مراجعة نهائية قبل الحفظ | UX | متوسطة | لا تعرض ملخصاً قبل اعتماد الملف. |
| 18 | لا يوجد ActivityLog عند فتح ملف | معماري | متوسطة | يوجد TimelineEvent فقط. |

### 11.6 قرار المراجعة

المعالج ينشئ دعوى أساسية، لكنه لا يحقق المعالج القانوني الكامل كما ورد في الخطة. أخطر مشكلة ليست فقط نقص خطوة المرفقات، بل أن تحديد الموعد القادم لا يتحول فعلياً إلى مهمة يومية بسبب طريقة تمرير البيانات إلى `TaskSyncService`.

### 11.7 خطة تعديل الشاشة لاحقاً

عند الوصول لمرحلة التعديل، يجب تنفيذ ما يلي:

1. إعادة بناء المعالج إلى 8 خطوات كما بالخطة الأصلية.
2. إضافة خطوة مرفقات أولية:
   - وكالة.
   - لائحة دعوى.
   - ثبوتيات.
   - صور مستندات.
3. فلترة الموكلين حسب `PersonRoles.client`.
4. فلترة الخصوم حسب `PersonRoles.opponent`.
5. فلترة الوكالات حسب الموكل المختار.
6. إضافة إمكانية إنشاء موكل/خصم/وكالة من داخل المعالج.
7. استخدام `CaseSubjects` و `Courts` و Lookups بدل القيم hardcoded.
8. إصلاح توليد المهمة اليومية عند تحديد الموعد القادم.
9. حفظ `nextAction` في الجلسة/المهمة الناتجة.
10. إضافة فحص سند تمثيل الشخص الاعتباري.
11. إضافة شاشة مراجعة نهائية قبل الحفظ.
12. فصل مسار “دعوى جديدة” عن “أرشفة دعوى سابقة”.
13. تسجيل ActivityLog إلى جانب TimelineEvent.

### 11.8 معايير قبول تعديل الشاشة مستقبلاً

- المعالج يحتوي 8 خطوات واضحة.
- لا يمكن اختيار وكالة لا تخص الموكل.
- الموكل والخصم مفلتران حسب الأدوار.
- يمكن إضافة شخص أو وكالة دون مغادرة المعالج.
- عند إدخال موعد قادم يظهر تلقائياً في الأعمال اليومية.
- يتم حفظ المطلوب للجلسة القادمة.
- يتم إنشاء نقص عند غياب الموعد أو الأساس أو الوكالة.
- يمكن إرفاق المستندات الأولية وربطها بالملف.
- يوجد ملخص نهائي قبل اعتماد الملف.

